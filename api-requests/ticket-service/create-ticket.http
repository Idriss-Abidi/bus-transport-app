### Create Ticket (via API Gateway)
@baseUrl = http://localhost:4004

POST {{baseUrl}}/api/tickets
Content-Type: application/json

{
  "trajetId": 1,
  "description": "Optional description for the ticket 1"
}
